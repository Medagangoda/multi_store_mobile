import 'package:flutter/material.dart';

class EaringScreen extends StatelessWidget {
  const EaringScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Earing Screen'),
    );
  }
}