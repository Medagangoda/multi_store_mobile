

import 'package:flutter/material.dart';

class AttributesTabScreens extends StatelessWidget {
  const AttributesTabScreens({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Attribute Screen'),
    );
  }
}